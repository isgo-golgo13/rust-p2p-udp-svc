[package]
name = "p2p"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
socket2 = "0.4"
dotenv = "0.15.0"
endpoints = { path = "../endpoints" }
